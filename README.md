# AI Music Recommendation System

## 1. Project Overview

This is a local Flask web project for a school AI Fair. It trains a hybrid music recommendation model from playlist data and user feedback.

This project does not use a local LLM. Recommendation explanations are generated by deterministic rules based on song features.

## 2. Why This Is Machine Learning

The system converts songs, playlists, and Like/Dislike feedback into training data. It builds a user-song interaction matrix, converts each song into item features, trains a recommendation model, and ranks candidate songs based on learned user preferences.

## 3. Tech Stack

- Python
- Flask
- pandas and NumPy
- scikit-learn fallback models
- LightFM open-source recommendation model
- Local CSV data storage
- Free music metadata APIs

## 4. Main Model: LightFM Hybrid Recommendation Model

The main model is LightFM, a hybrid recommendation model trained on user-song interactions and item features.

The project uses:

```python
LightFM(loss="warp")
```

WARP is useful for Top-N recommendation because it trains the model to rank relevant songs higher than less relevant songs.

If LightFM is not installed or cannot be trained yet, the app falls back to a content-based baseline and a LogisticRegression feedback model when enough Like/Dislike examples exist.

## 5. Dataset Structure

The app automatically creates these files:

- `data/songs.csv`
- `data/playlists.csv`
- `data/interactions.csv`
- `data/feedback.csv`

`songs.csv` stores metadata and audio-style features such as danceability, energy, valence, tempo, acousticness, speechiness, and popularity.

`interactions.csv` stores training events:

- `playlist_import = 1.0`
- `manual_add = 1.0`
- `like = 2.0`
- `dislike = -1.0`
- `skip = -0.5`

## 6. Playlist Import

QQ Music import is used only for playlist metadata import in an educational project. The system does not download music, bypass copyright restrictions, or provide unauthorized playback.

If a QQ Music song matches the local demo dataset, the app can reuse local audio features. If it does not match, the song is saved as metadata only and is not used directly for model training.

## 7. Free Music Search APIs

Search order:

1. Local CSV search
2. MusicBrainz search
3. Last.fm search, if `LASTFM_API_KEY` is set in `.env`
4. Deezer search

Online API results usually provide metadata only. If a result does not have audio features, the page shows that it cannot be used directly for model training.

## 8. Feature Engineering

Songs are converted into LightFM item feature tokens, for example:

- `genre:pop`
- `artist:the_weeknd`
- `tag:dance`
- `source:demo`
- `energy:high`
- `valence:positive`
- `tempo:fast`
- `danceability:high`
- `acousticness:high`
- `speechiness:high`
- `popularity:high`

## 9. Training Process

1. Load songs with audio features.
2. Load playlist, manual add, Like, and Dislike events.
3. Keep positive interactions for LightFM WARP training.
4. Build the user-item interaction matrix.
5. Build the item feature matrix.
6. Train the LightFM hybrid recommendation model.
7. Save the trained model to `models/lightfm_model.pkl`.

## 10. Recommendation Process

If LightFM is trained:

```text
final_score = 0.7 * lightfm_score + 0.3 * content_similarity_score
```

If LightFM is unavailable:

```text
final_score = 0.6 * content_similarity_score + 0.4 * logistic_regression_probability
```

The system excludes songs the user already imported, manually added, liked, or disliked.

## 11. Feedback Learning

When the user clicks Like or Dislike:

1. The feedback is saved to `data/feedback.csv`.
2. A new interaction is saved to `data/interactions.csv`.
3. The app tries to retrain the model.
4. Future recommendations can change based on the new feedback.

## 12. How to Run Locally

Mac / Linux:

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Windows:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Open the URL printed in the terminal, such as:

```text
http://127.0.0.1:5000
```

If port 5000 is busy, the app automatically tries 5001, 5002, 5003, 8000, and 8080.

## 13. How to Use

1. Import a QQ Music playlist, or add songs manually.
2. Search songs and add local training-ready songs.
3. Click Train Model.
4. Click Get Recommendations.
5. Like or Dislike recommended songs.
6. Retrain and compare how the results change.

## 14. AI Fair Presentation Script

This project trains a hybrid music recommendation model from playlist data and user feedback. Playlist import creates implicit feedback, while Like and Dislike create explicit feedback. Each song is represented by item features such as genre, artist, mood, tempo, and energy. The LightFM model learns which songs are more likely to match the user's taste and ranks songs for recommendation.

## 15. Limitations

- Free online music APIs usually do not provide full audio features.
- QQ Music import depends on public metadata availability.
- The demo dataset uses realistic but simplified audio feature values.
- The model improves when the user provides more playlist and feedback data.

## 16. Future Improvements

- Add a larger licensed audio-feature dataset.
- Support multiple demo users.
- Add evaluation metrics such as precision@k and recall@k.
- Add better duplicate detection across music platforms.

## 17. Copyright and Safety Note

This project only imports playlist metadata for educational recommendation-system research. It does not download music, unlock paid content, bypass platform restrictions, or provide unauthorized playback.

Optional note: `qqmusic-api-python` can be explored as a non-official metadata helper, but it is not required and is not included as a required dependency.
